- first run: json-server --watch db.json --port 5000 (fake database for now)
- on another terminal: npm run dev

to switch between admin/instructor/coordinator: 
- logout
- go to db.json file to search for credentials, enter specific email and pass for the role you want in the logout page.